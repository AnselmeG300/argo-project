apiVersion: v1
kind: Service
metadata:
  name: demo-web
  namespace: demo
  labels:
    app: demo-web
spec:
  type: ClusterIP
  selector:
    app: demo-web
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
